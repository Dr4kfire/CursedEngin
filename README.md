# CursedEngin
It's a terminal based C++ game engine for making console based games using the PDCursesMod library!

# Compilation
Make sure you have the PDCursesMod library compiled and that the library file is in the `lib/` folder
Compile this project using CMake - there is a CMakeLists.txt provided!
